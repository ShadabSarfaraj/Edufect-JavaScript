// Write a function to calculate square of a number and return it.
function square(num) {
  let ans = num * num;
  return ans;
}
square(4); // function will return 16 which is square of 4 but it will not print it
console.log(square(5));
let sq = square(6); // functio will return 36 and it will be stored in sq variable
console.log(sq);
